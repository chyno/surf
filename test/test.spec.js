	var assert = require("assert");
	
	describe("First Tests", function() {
		describe("Get something to work", function() {
			it("first test", function() {
				let foo = {bar: "dd"}
				assert(foo);
			})
		});
	});